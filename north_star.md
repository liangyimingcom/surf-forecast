# North Star — v4 三屏收敛版 · ✅ 档落地 web/frontend

> **视觉规格 = `docs/design-v4-update1.html`**（用户已选定的三屏收敛版原型，628 行、0 JS 报错）。
> 它就是本轮的 spec：颜色/间距/字体/排版/交互形态照它做，**不再另出 mockup**。
> 索引与两版差异见 `docs/design-directions.html`。
> 事实来源仍是 `docs/HANDOFF-to-kiro.md`。

## 一句话目标

把三屏原型里**已有真实数据支撑的那一档**，落到现有 Vue3 决策助手（`web/frontend`）上：
让线上从"普通蓝白 SPA"变成原型那种**晨报纸感**的三屏，且**每个数字都有真实字段撑着**。

## 范围（只做这一档，逐项列全，不多不少）

**首页**：verdict 大标题 · 覆盖计数(fresh/total) · 本周走势 sparkline · 校准时间戳链到 `/status`
**小白**：一屏答案（分/标签/窗口/板型）· 最佳窗口倒计时 · 几点出门倒推(通勤) · 行动建议
**高手**：五维 dims · 双周期图 · 风向罗盘 · 物理小课堂 · 昨日回看 · m/ft 单位切换
**全局**：晨报纸感设计令牌 + 夜读模式

### 现状盘点（2026-08-05 实测，决定工作量）

| 状态 | 项 |
|------|-----|
| ❌ 完全没有，要新建 | 本周走势 sparkline · 倒计时 · 通勤倒推 · 风向罗盘 · m/ft 单位切换 · 夜读模式 · CSS 设计令牌（`style.css` 里 0 个变量） |
| ⚠️ 已有但要改造成纸感 | 五维 dims · ChartBox 双周期图 · 物理小课堂 · 昨日回看 · 校准时间戳 |

## 硬围栏（越界即停，不许绕）

1. **后端零改动**：本档所有数据都来自现有 `/api/report`·`/api/recommend`·`/api/catalog` 字段。
   **不得修改 `src/`**。若发现某项缺字段 → 说明缺什么、停下问，**不许在前端造值**。
2. **不做 🟡🟠🔴 三档**（下轮再议，本轮碰即越界）：
   - 🟡 需后端补字段：首光日落 `sunrise/sunset` · 水温 `sst` · 月相 `moonPhase`
   - 🟠 需新后端能力：现场众报投票 · 入水报备 · 多浪点横评
   - 🔴 **无数据源，做了就是编造**：今日注意告警 · 浪点设施 · 浪点 DNA 契合% ·
     涌浪溯源 · 浪点季节志 · 人流「偏多」
   原型里有这些块 → **照抄视觉但不许填假数**：整块不做，或做成明确标注的「二期」占位。
3. **数据诚实**：缺值显示为空或「—」，绝不用占位数字/示例值冒充真实数据；
   降级态照旧显 `fresh/total` 不冒充。
4. **设计令牌，不硬编码**：颜色/间距/字号进 `style.css` 的 CSS 变量；夜读模式靠变量切换，
   不得逐组件写死颜色（否则夜读模式必破）。
5. **不推 master**；只推 feature 分支 + 开 PR。

## 硬门（loop 必须停下交人，不得尝试）

| 门 | 内容 |
|----|------|
| 🔒 G1 | 任何生产数据写入（DynamoDB） |
| 🔒 G2 | 任何部署/回滚/task def 变更 |
| 🔒 G3 | `terraform apply` |
| 🔒 G4 | 合并 PR 到 master |
| 🔒 G5 | 改 `src/` 后端（本轮定义为越界，见围栏 1） |

## 完成即停

S0~S5 全部勾选 + pytest 不倒退（基线 **329**）+ `vue_spa.mjs` E2E 全绿（基线 **32**，需扩断言）
+ 三屏截图与原型逐屏对照 + 新用户可用性评审已折回 → **创建停止文件并汇报**。

停止文件路径以 **auto-nudge 消息里给出的那个为准**（上一轮是
`/Users/yiming/Downloads/all_the_meshclaw/surf-forecast/.stop-chat-3-1783779532`）；每轮开工先查它。

若同一处连续两轮做不对：停止微调，说明根因、换路线，必要时开门问人。
