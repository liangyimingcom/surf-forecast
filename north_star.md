# North Star — 数据健康收口（本轮 loop 目标）

> **事实来源：[`docs/HANDOFF-to-kiro.md`](docs/HANDOFF-to-kiro.md)**（含 §7 交接后更新）。
> 现状/时间线/运维血泪只在那份文档维护一份，本文件不复述，只回答：
> **这一轮做什么 · 什么绝对不做 · 什么时候算完**。

## 一句话目标

把「刷新 60/60 全绿但某浪点实际不可用」这个**监控可信度洞**堵住：
让 `/status`（本项目唯一的故障发现渠道）能自己发现 **①产出空报告 ②坐标非法 ③坐标重复歧义**
这三类静默故障，而不是靠人肉逐点翻 S3。

## 为什么是这个目标

2026-08-05 修完 sl75/sl76 后刷新报 **60/60 · failed=[]**，但 `coverage` 仍是 **pool 37 / fresh 36**
——`sl82 Canggu` 坐标正确、上游 WAM025 格点 `-8.75/115.25` 却返回 48 时点全空，
引擎产出 `days: 0`，而 manifest **把"写出了 latest.json"就算 succeeded**。
即：**绿灯不等于可用**。这正是「无推送告警、`/status` 是唯一发现渠道」这个决策最怕的漏洞。

同期还查出注册表已有 3 组重复坐标（`sl49/sl93`、`sl54/sl84`、`sl2/sl58`），
`find_registry_by_coord` 静默取首个匹配 —— 与 v0.3.2 那个「缓存从未命中」bug 同族。

## 本轮授权边界（loop 可自主做的）

✅ 改 `src/` 代码 + 加 `tests/` 单测 + 写 `tools/` 只读巡检脚本 + 改文档 + 建分支/提交/开 PR。

## 硬门（loop 必须停下来交给人，**不得尝试**）

| 门 | 内容 |
|----|------|
| 🔒 G1 | **任何生产数据写入**（DynamoDB 改坐标/开关：Canggu、重复坐标、Kirra 都属此列） |
| 🔒 G2 | **任何部署/回滚/task def 变更**（含配置 `SF_TEST_ACCESS_KEY`） |
| 🔒 G3 | `terraform apply` |
| 🔒 G4 | **合并 PR 到 master**（开 PR 可以，点 merge 是人的事） |
| ⛔ G5 | **设计方向 v4~v7 的选择**（`docs/design-directions.html`，等用户拍板，loop 不得自选自实施） |

遇到硬门：写清"要做什么、为什么、怎么回退"，**然后停**，不要绕路、不要"先做了再说"。

## 不可妥协（红线，任何目标都不得违反）

1. **数据诚实**：陈旧/缺失/不可用一律显式降级（`degraded` + `fresh/total`），绝不用旧分冒充当日分。
2. **绿灯必须等于可用**：本轮的核心命题——`succeeded` 不得包含产不出可用报告的点。
3. **可见性不耦合刷新开关**：公开目录/直播用 `list_listed_registry`（仅 `status=active`），
   刷新/回收用 `list_active_registry`。冷点回收只回收 `source=="user"` 自建点。
4. **taskdef 永远用 family 级 ARN**（钉 revision 会让 EventBridge 静默 AccessDenied，07-27/28 事故根因）。
5. **合规**：`/api/cams` 直播源为逆向所得（研究用途），必须保持登录后可取、不对匿名公开。
6. **单驱动**：loop 运行期间人不并行改这个仓库；不启第二个驱动器。
7. **不推 master**：只推 feature 分支，PR 交人合。

## 完成即停

R1~R5 全部勾选 + `pytest` 全绿 + `vue_spa` E2E 全绿 + PR 已开 → **创建 `STOP_LOOP` 并汇报**，
把所有 🔒 门项整理成一份"待人工确认清单"。

若同一问题连续两轮修不好：**停止微调，说明根因、换一条路线**，必要时开门问人。
