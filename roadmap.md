# Roadmap — v4 三屏 ✅ 档落地 S0~S5

> 目标与围栏见 `north_star.md`；复选框见 `tasks.md`；视觉规格 = `docs/design-v4-update1.html`。
> **顺序执行**，每阶段都以「截图与原型对照」收尾——看不见就等于没做对。
> 资源紧张时跑定向测试，S5 才跑全量。

## S0 · 起手 + 对照基线

1. 查停止文件（路径见 auto-nudge 消息）；存在即停。
2. 在 `master` 且已 pull（三件套随 PR #43 合入 master）；建分支 `feat/v4-three-screen-ui`。
3. 取基线：`pytest -q`（应 **329**）、`vue_spa.mjs`（应 **32/0**）。
4. **把原型当规格读一遍**：在浏览器打开 `docs/design-v4-update1.html`，
   截三屏图存 `docs/screenshots/`，作为后续每阶段的比对基准。

**验收**：分支就位、基线数字记录在案、原型基准截图已存。

## S1 · 晨报纸感设计令牌 + 夜读模式

`style.css` 现在**一个 CSS 变量都没有**，颜色散落各组件——先把地基铺好，否则夜读模式必破。

- 从原型抽令牌：纸底/卡片/墨色/次级墨/分隔线/海蓝/暖橙/警橙 + 衬线数字字族 + 圆角/阴影。
- 夜读模式：`body.night` 覆盖同一批变量 + localStorage 持久化 + 顶部开关（原型那颗 🌙）。
- 现有四页改为引用变量（**不留硬编码颜色**）。

**验收**：日/夜两态各截一张全站图；用 `getComputedStyle` 抽查 3 个组件确认取的是变量值而非写死色；
E2E 加一条夜读模式持久化断言（刷新后仍是夜色）。

## S2 · 首页 → 晨报决策首页

- verdict 大标题：原型那句「这周去 X，周三早上最值得」（数据 = `recommend.best`）。
- 覆盖计数 `fresh/total`（已有逻辑，改成原型排版）+ 降级态照旧诚实显示。
- **本周走势 sparkline**（新建）：数据 = 详情 `days[].score`；原型是折线 + 峰值标注。
- 校准时间戳（`calibratedAt`）链到 `/status`，文案照原型「先验证过去，再相信未来 → 数据健康」。
- 原型首页里属 🔴 档的块（今日注意告警 / 现场众报 / 三点横评的车程与人流列）→ **不做**。

**验收**：首页截图与原型①对照；sparkline 峰值与 `days[].score` 最大值一致；E2E 断言走势与链接存在。

## S3 · 详情页 · 小白模式

- 一屏答案（分/标签/窗口/板型/一句话建议）改成原型排版。
- **最佳窗口倒计时**（新建）：由 `window` + 当前时间算，秒级跳动；窗口已过要有正确措辞。
- **几点出门倒推**（新建）：通勤时长可调（±）并存 localStorage，倒推「出门 → 下水」；纯前端，不碰后端。
- 行动建议（`plan`）改排版。
- 属 🟡🔴 档的块（水温/穿着、入水报备、浪点设施、AI 教练那句含首光的话）→ **不做或标二期占位**。

**验收**：小白屏截图与原型②对照；倒计时跨窗口边界（窗口前/窗口中/窗口后）三态文案正确并有单测或 E2E 断言。

## S4 · 详情页 · 高手模式

- 五维 dims + 双周期图（复用 `charts.js`/`ChartBox`）改纸感配色，**图表取变量色**以适配夜读。
- **风向罗盘**（新建）：扇区 = 接浪朝向 `spotFacingDeg`，三支箭 = 06/12/18 时风矢量（`wdeg`）；
  判定复用 `charts.js` 的 `windKind`，**不要另写一套阈值**（否则前后端口径会漂）。
- 物理小课堂（`lesson`）+ 昨日回看（`history`，含自评）改排版。
- **m/ft 单位切换**（新建）：影响全部浪高显示，存 localStorage。
- 属 🔴 档的块（浪点 DNA 契合% / 涌浪溯源 / 季节志）→ **不做**。

**验收**：高手屏截图与原型③对照；罗盘的离岸/侧岸/向岸判定与 `windKind` 一致（加断言）；
单位切换后所有浪高数字同步变化。

## S5 · 验收收口

1. `pytest -q` 不倒退（基线 329；本轮后端零改动，数字应不变）。
2. `vue_spa.mjs` 全绿 + 0 JS 报错，并**扩断言覆盖本轮所有新块**（走势/倒计时/通勤/罗盘/单位/夜读）。
3. 三屏截图与原型逐屏对照，贴进 PR。
4. **新用户零上下文可用性评审**（独立子代理，无本轮上下文）：可发现性 / 措辞是否有黑话 /
   不看文档能否自解释；发现折回代码后再重截图。
5. 文档回写：`docs/implementation-notes.md` 逐阶段记录（含偏离理由）+ `HANDOFF-to-kiro.md` §7 追加。
6. 开 PR（**不合并**，🔒 G4）；整理 🔒/🟡🟠🔴 待办清单；创建停止文件并汇报。

## 本轮之后（不做）

🟡 后端补 `sunrise/sunset`·`sst`·`moonPhase` → 解锁首光/穿着/潮历 ·
🟠 众报投票/入水报备/多点横评 → 需新表与端点 ·
🔴 告警/设施/DNA/溯源/季节志 → 需新数据源，**在有源之前不做** ·
上生产走 G 门（build → rollback 切版 → canary → smoke → tag → CHANGELOG）。
